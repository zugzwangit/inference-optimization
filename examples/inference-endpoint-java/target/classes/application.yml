server:
  port: 8080

spring:
  application:
    name: inference-endpoint

# Placeholders for the black-box dependencies. In a real deployment these would point at the
# control plane (for live model versions / SLO tiers) and the data plane (serving fabric).
inference:
  control-plane:
    # base-url: https://control-plane.internal
    refresh-interval-seconds: 30
  data-plane:
    # base-url: https://data-plane.internal
    request-timeout-ms: 60000
